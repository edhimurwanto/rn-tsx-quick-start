import * as React from 'react'
import AppNavigation from '@navigations/AppNavigation'

interface AppProps { }

const App: React.FunctionComponent<AppProps> = () => <AppNavigation />

export default App